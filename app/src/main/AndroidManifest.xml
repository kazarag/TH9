<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bai1AdvancedControls"
        tools:targetApi="31">
        <activity android:name=".TimeSelection" />
        <activity android:name=".AutocompleteTextView" />
        <activity android:name=".ContextMenuActivity" />
        <activity android:name=".TabSelectorActivity" />
        <activity android:name=".GridviewActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".ListviewActivityWithArrayList" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ArrayAdapterDemo" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>